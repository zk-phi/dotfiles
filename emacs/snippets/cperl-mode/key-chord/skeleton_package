# name : skeleton (package)
# key : kc-sk
# condition : (and buffer-file-name (string-match "\\.pm$" buffer-file-name))
# --
package ${1:Package::Name};

use strict;
use warnings;
use utf8;

use parent qw/${2:Class::Accessor::Fast}/;

$0

1;

__END__
