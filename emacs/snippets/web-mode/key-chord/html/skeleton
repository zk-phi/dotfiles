# name : skeleton
# key : kc-sk
# condition : (and (string= (web-mode-language-at-pos) "html") (= (point-min) (point-max)))
# --
<!DOCTYPE html>
<html>

  <head>
    <title>${1:Page Title}</title>
    <meta charset="${2:UTF-8}">
    <meta name="viewport" content="initial-scale=1.0">
    <meta name="description" content="${3:Hogehoge}">
    $0
  </head>

  <body>

  </body>

</html>
